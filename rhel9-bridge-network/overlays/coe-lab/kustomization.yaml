apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: rguske-rhel9-bridge-gitops
# namePrefix: coe-

resources:
  - ../../base

patchesStrategicMerge:
  - patch-network.yaml
  - patch-storage.yaml